<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" 
    integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <title>Library</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Home</a>
        <a class="navbar-brand" href="/books">Books</a>
        <a class="navbar-brand" href="/authors" <%= author ? 'hidden' : '' %> >Authors</a>
        <% if(username){ %>
          <a class="navbar-brand" href="/users/<%=username%>"><%= username %></a>
          <a class="navbar-brand" href="/users/logout">Logout</a>
        <% } else if (author) { %>
          <a class="navbar-brand"><%= author %> </a>
          <a class="navbar-brand" href="/users/logout">Logout</a>
        <% } else { %>
          <a class="navbar-brand" href="/users/login">Login</a>
        <% } %>
        </div>
      </nav>
    <div class="container mt-4 text-center">
        <h1><%= title %></h1>
    </div>